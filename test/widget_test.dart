import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('DEDO app smoke test', (WidgetTester tester) async {
    // Minimal smoke test — full integration requires Hive initialization
    expect(true, isTrue);
  });
}
